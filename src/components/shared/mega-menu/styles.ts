export const appBarStyles = {
  position: "static",
  color: "default",
  elevation: 0,
};

export const toolbarStyles = {
  // Default toolbar styles - can be extended if needed
};

export const containerStyles = {
  flexGrow: 1,
};

export const shopButtonStyles = {
  // Default button styles - can be extended if needed
};

export const menuStyles = {
  // Default menu styles - can be extended if needed
};

export const menuItemStyles = {
  // Default menu item styles - can be extended if needed
};
